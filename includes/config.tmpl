<script>
  window.eyeo = {
    {% if disable_stripe %}
    disableStripe: true,
    {% endif %}
    {% if disable_paypal %}
    disablePayPal: true,
    {% endif %}
    excludeCookiePrompt: {{ "true" if exclude_cookie_prompt else "false" }},
    enableSplitTesting: {{ "true" if enable_split_testing else "false" }},
    splitTestingVariants: {{ "true" if split_testing_variants else "false" }},
    testAnalytics: {{ "true" if analytics_testing else "false" }},
    testOptimize: {{ "true" if optimize_testing else "false" }}
  }

  window.console = window.console || {warn: function(){}};
</script>
