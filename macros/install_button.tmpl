{% macro install_button(install_suffix, label_text, classes) %}
  <a
    id="install-button"
    data-install-suffix="{{ install_suffix }}"
    class="{{ classes }} download-button go-to-download"
    href="download">
    {{ label_text }}
  </a>
{% endmacro %}
